# agrisentinal
This Python tool parses and analyzes heavy .hdr/.raw hyperspectral crop imagery. It isolates vegetation by calculating NDVI to filter out soil noise, then feeds the canopy data into a machine learning model. Processing 100k+ pixels at once, it catches early crop stress and visualizes field health in a detailed diagnostic heatmap.
